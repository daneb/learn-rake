task :default do
  puts 'Sample Rake for Database Management'
end

namespace :db do
  desc "Builds the SQLite DB"

  task :create => [:task1, :task2] do
    puts "Creating database and implementing schema"
  end

  desc "Populate the DB with mock records"
  task :populate do

  end

  desc "Clean the DB and remove the file store"
  task :clean do

  end
end
